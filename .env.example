# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=dcrs_db
DB_SSL=false

# Security & Secrets
JWT_SECRET=dcrf_jwt_secret_key_change_in_production
CRON_SECRET=dcrf_cron_secret_trigger

# AI Assistant Cloud Key (Optional locally; if not provided, chatbot falls back to local Ollama)
NVIDIA_API_KEY=
